var roomController = require('./roomController.js');

module.exports = function(app) {
	// app === linkRouter injected from middleware.js
  
  /**
	 * TODO: need to get router set up in middleware.js
	 * call fn's created in room controller
	 *
   */ 
};
